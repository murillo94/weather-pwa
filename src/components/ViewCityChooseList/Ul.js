import styled from 'styled-components';

const Ul = styled.ul`
	display: flex;
	flex-direction: row;
	align-items: center;
	list-style: none;
	margin: 0;
	padding: 0;
	border-top: 1px dashed #ffffff;
	padding: 2em 1em;
`;

export default Ul;